server:
  port: 8082
spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5433/rsbank
    username: postgres
    password: root
  jpa: 
    hibernate:
      ddl-auto: none
    show-sql: true
  sql:
    init:
      mode: always
      
logging:
  level:
    org:
      hibernate:
        sql: DEBUG

build:
  version: "1.0"

cards:
  message: "Welcome to RSBank cards related local APIs from docker DB "
  contactDetails:
    name: "John Doe - Developer"
    email: "john@rsbank.com"
  onCallSupport:
    - (555)555-1234
    - (555)523-1345